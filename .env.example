# Slack webhook URL for notifications (optional)
# Get yours from: https://api.slack.com/messaging/webhooks
SLACK_WEBHOOK_URL=

# Anthropic API key (only needed if using --llm-fallback)
ANTHROPIC_API_KEY=
